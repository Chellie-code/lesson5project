let diameterInput = prompt('Enter diameter of circle');
let radiusInput = propmt('Enter radiu of circle');

function calculateArea(diameterInput,radiusInput){
   let area = diameter * radiusInput;
   alert(`Area of your circle is ${area}`);
   //alert('Area of your circle is' + area)
}

calculateArea(diameterInput,radiusInput);
